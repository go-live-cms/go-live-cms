---
import AdminLayout from "@/layouts/gl-admin/Layout.astro"
import App from "@gl-admin/app"

export async function getStaticPaths() {
  return [{ params: { slug: "" } }, { params: { slug: "*" } }]
}
---

<AdminLayout>
  <App client:only="react" />
</AdminLayout>
